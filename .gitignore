CLAUDE.md
.claude

# Python
.venv/
__pycache__/
*.pyc

# Содержимое raw_pages/ игнорируется самим raw_pages/.gitignore (узнаваемые
# сырые тексты вики в репозиторий не попадают), но папка остаётся в дереве,
# чтобы пайплайн fetch → anonymize воспроизводился из чистого клона.
